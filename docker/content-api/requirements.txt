# Minimal runtime deps for the DynamoDB content read API only.
# Do not install the full pyproject.toml tree (pipeline / LLM packages).
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
boto3>=1.34.0
python-dotenv>=1.0.0
pydantic>=2.0.0
